apiVersion: apps/v1
kind: Deployment
metadata:
  name: skys-cli-app-dep
spec:
  selector:
    matchLabels:
      app: skys-cli-app
  strategy:
    type: Recreate
  template:
    metadata:
      name: skys-cli-app-pod
      labels:
        app: skys-cli-app
    spec:
      containers:
        - name: skys-cli-app
          image: skys-cli:nodejs
          imagePullPolicy: Never
          volumeMounts:
            - name: skys-cli-app
              mountPath: /var/www
            - name: skys-cli-app-npm-log
              mountPath: /root/.npm/_logs
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "development"
            - name: AWS_DEFAULT_REGION
              value: "ap-northeast-1"
            - name: AWS_DEFAULT_OUTPUT
              value: "json"
            - name: AWS_ACCESS_KEY_ID
              value: "test"
            - name: AWS_SECRET_ACCESS_KEY
              value: "test"
            - name: AWS_CLI_FILE_ENCODING
              value: "utf-8"
            - name: DATABASE_HOST
              value: "skys-dynamodb-svc"
            - name: DATABASE_PORT
              value: "8000"
            - name: MEDIA_TABLE_NAME
              value: "Media"
            - name: NEWSFEED_TABLE_NAME
              value: "Newsfeed"
      volumes:
        - name: skys-cli-app
          hostPath:
            path: {{ .Values.container.volume.project_root_path }}
            type: DirectoryOrCreate
        - name: skys-cli-app-npm-log
          hostPath:
            path: {{ .Values.container.volume.project_root_path }}/logs/npm
            type: DirectoryOrCreate
