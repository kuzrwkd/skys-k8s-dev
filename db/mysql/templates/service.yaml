apiVersion: v1
kind: Service
metadata:
  name: dev-skys-api-db-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: dev-skys-mysql
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      nodePort: {{ .nodePort }}
    {{- end }}
