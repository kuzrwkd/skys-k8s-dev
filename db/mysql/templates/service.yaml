apiVersion: v1
kind: Service
metadata:
  name: dev-skys-api-db-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: dev-skys-mysql
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}