apiVersion: v1
kind: Service
metadata:
  name: dev-skys-dynamodb-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: dev-skys-dynamodb
  ports:
    {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      nodePort: {{ .nodePort }}
    {{- end }}