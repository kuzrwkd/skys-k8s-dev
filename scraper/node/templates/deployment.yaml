apiVersion: apps/v1
kind: Deployment
metadata:
  name: dev-skys-scraper-app-dep
spec:
  selector:
    matchLabels:
      app: dev-skys-scraper-app
  strategy:
    type: Recreate
  template:
    metadata:
      name: dev-skys-scraper-app-pod
      labels:
        app: dev-skys-scraper-app
    spec:
      containers:
        - name: dev-skys-scraper-app
          image: dev-skys-scraper
          imagePullPolicy: Never
          volumeMounts:
            - name: dev-skys-scraper-app
              mountPath: /var/www
            - name: dev-skys-scraper-app-npm-log
              mountPath: /root/.npm/_logs
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: "development"
            - name: AWS_DEFAULT_REGION
              value: "ap-northeast-1"
            - name: AWS_DEFAULT_OUTPUT
              value: "json"
            - name: AWS_ACCESS_KEY_ID
              value: "localstack"
            - name: AWS_SECRET_ACCESS_KEY
              value: "localstack"
            - name: AWS_CLI_FILE_ENCODING
              value: "utf-8"
            - name: DYNAMODB_HOST
              value: "localstack"
            - name: DYNAMODB_PORT
              value: "4566"
      volumes:
        - name: dev-skys-scraper-app
          hostPath:
            path: {{.Values.container.volume.project_root_path}}/app
            type: DirectoryOrCreate
        - name: dev-skys-scraper-app-npm-log
          hostPath:
            path: {{.Values.container.volume.project_root_path}}/app/logs/npm
            type: DirectoryOrCreate
