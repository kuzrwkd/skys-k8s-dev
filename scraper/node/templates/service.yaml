apiVersion: v1
kind: Service
metadata:
  name: dev-skys-scraper-app-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: dev-skys-scraper-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      nodePort: {{ .Values.service.nodePort }}